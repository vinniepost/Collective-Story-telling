using UnityEngine;

// Obsolete: CodeRedManager now receives server events directly from WebSocketController.
// This stub exists only to prevent compile errors if the component is still present in a scene.
public class CodeRedSync : MonoBehaviour
{
    // Intentionally empty
}
